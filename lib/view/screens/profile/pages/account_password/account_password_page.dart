import 'package:flutter/material.dart';

class AccountPasswordPage extends StatelessWidget {
  const AccountPasswordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
