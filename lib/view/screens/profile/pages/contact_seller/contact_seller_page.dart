import 'package:flutter/material.dart';

class ContactSellerPage extends StatelessWidget {
  const ContactSellerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
