import 'package:flutter/material.dart';

class ContactSellerWithKeyboardPage extends StatelessWidget {
  const ContactSellerWithKeyboardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
