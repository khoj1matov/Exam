import 'package:flutter/material.dart';

class LogInForgotPasswordAuth extends StatelessWidget {
  const LogInForgotPasswordAuth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
