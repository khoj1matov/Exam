import 'package:flutter/material.dart';

class SettingsTermsCart extends StatelessWidget {
  const SettingsTermsCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
