import 'package:flutter/material.dart';

class SettingsPurchasedCart extends StatelessWidget {
  const SettingsPurchasedCart({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}