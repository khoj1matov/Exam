import 'package:flutter/material.dart';

class SettingsReportCart extends StatelessWidget {
  const SettingsReportCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
