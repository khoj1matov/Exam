import 'package:flutter/material.dart';

class SettingsPrivacyCart extends StatelessWidget {
  const SettingsPrivacyCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
