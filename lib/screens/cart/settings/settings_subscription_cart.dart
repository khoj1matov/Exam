import 'package:flutter/material.dart';

class SettingsSubscriptionCart extends StatelessWidget {
  const SettingsSubscriptionCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
