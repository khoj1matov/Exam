import 'package:flutter/material.dart';

class NewsCart extends StatelessWidget {
  const NewsCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
