import 'package:flutter/material.dart';

class SplashScreenSecondView extends StatelessWidget {
  const SplashScreenSecondView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
