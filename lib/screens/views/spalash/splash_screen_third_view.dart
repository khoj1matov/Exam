import 'package:flutter/material.dart';

class SplashScreenThirdView extends StatelessWidget {
  const SplashScreenThirdView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
