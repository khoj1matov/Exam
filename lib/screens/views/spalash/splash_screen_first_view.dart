import 'package:flutter/material.dart';

class SplashScreenFirstView extends StatelessWidget {
  const SplashScreenFirstView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
