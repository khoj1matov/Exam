import 'package:flutter/material.dart';

class SubscriptionAcceptView extends StatelessWidget {
  const SubscriptionAcceptView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
